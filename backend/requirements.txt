fastapi==0.109.0
uvicorn[standard]==0.27.0
sqlmodel==0.0.14
pydantic>=2.10.0
pydantic-settings>=2.0.0
pydantic-ai>=0.0.14
# pydantic-ai 0.4.x importuje prywatne opentelemetry._events, usunięte w api 1.44.0
# (backend nie wstawał po rebuildzie 2026-08-11); para api/semconv musi być spójna,
# a semconv >=0.60b1 wymaga mistralai ciągnięty przez pydantic-ai
opentelemetry-api==1.39.1
opentelemetry-semantic-conventions==0.60b1
asyncpg==0.29.0
aiohttp>=3.9.0
httpx>=0.27.2
beautifulsoup4==4.12.3
alembic==1.13.1
python-dotenv==1.0.0
# redis==5.0.1  # REMOVED (2026-02-17): Not used - all cache in PostgreSQL
# celery==5.3.6  # REMOVED (2026-02-17): Not used - using APScheduler instead
lxml==5.1.0
apscheduler==3.10.4
openai>=1.12.0
feedparser>=6.0.10
jupyterlab
pandas
matplotlib
sqlalchemy
psycopg2-binary

# Auth (Sprint 1)
python-jose[cryptography]>=3.3.0
passlib>=1.7.4
bcrypt>=4.0.0,<4.1.0
email-validator>=2.0.0

# Newsletter (Sprint 2)
resend>=0.7.0
jinja2>=3.0.0

# Push Notifications (Sprint 5C)
pywebpush>=2.0.0

# BIP Scraper (Sprint 5D)
pdfplumber>=0.11.0

# Gemini Maps grounding (Sprint 8)
google-genai>=1.47.0

# Karta dnia na Facebooka — grafika składana lokalnie (services/social_card.py)
pillow>=10.0.0

# Sesje Rady Gminy — pobieranie audio z YouTube (services/council_transcript.py).
# Wymaga też ffmpeg w systemie: patrz apt-get w Dockerfile.
# YouTube zmienia zasady kilka razy w roku — przy błędzie 403 zacznij od podbicia
# tej wersji, dopiero potem grzeb w `_YTDLP_ARGS`.
yt-dlp>=2025.10.14
